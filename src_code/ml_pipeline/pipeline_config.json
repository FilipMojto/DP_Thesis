{
    "phases": [
        {
            "name": "hyperparameter-tuning",
            "skip": false,
            "models": [
                "RF",
                "XGB"
            ],
            "description": "Hyperparameter tuning for specified models using the preprocessed training data. Possible models: RF, XGB.",
            "kwargs": {}
        },
        {
            "name": "preprocessing",
            "skip": true,
            "subsets": [
                "train",
                "val",
                "test"
            ],
            "description": "Final data preprocessing steps after hyperparameter tuning. Output dataset is ready for model training. Possible subsets: train, val, test.",
            "engineering_kwargs": {
        
            },
            "transformation_kwargs": {}
        },
        {
            "name": "training",
            "skip": true,
            "models": [
                "RF",
                "XGB"
            ],
            "description": "Train specified models using the tuned hyperparameters on the preprocessed training data. Possible models: RF, XGB.",
            "kwargs": {
                "load_tuned": false,
                "skip_pfi": false
            }
        },
        {
            "name": "evaluation",
            "skip": true,
            "description": "Evaluate trained models on validation and test datasets.",
            "kwargs": {}
        }
    ]
}